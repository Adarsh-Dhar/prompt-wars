# Get this from Google AI Studio
# https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# EXPORT FROM PHANTOM: Settings -> Export Private Key
# It should look like a long array of numbers or a base58 string
# IMPORTANT: Use a BURNER wallet, never your main wallet!
SOLANA_PRIVATE_KEY=your_private_key_here

# Use a fast RPC (Helius, QuickNode) or default public one for testing
# Examples:
# - https://api.mainnet-beta.solana.com (public, slower)
# - https://mainnet.helius-rpc.com/?api-key=YOUR_KEY (faster, requires signup)
RPC_URL=https://api.mainnet-beta.solana.com

# x402 Server Configuration
# Your receiving wallet address (where x402 payments go)
SERVER_WALLET=YOUR_RECEIVING_WALLET_ADDRESS

# Price per request in SOL for premium endpoints
PRICE_SOL=0.001
